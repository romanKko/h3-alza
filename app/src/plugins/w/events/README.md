# Required third-party plugins
`$ npm i mitt@2`

# Import in `main.ts`
```javascript
import events from '@/plugins/w/events'

const app = createApp(App)
    .use(events)
```
# Usage
!!!! DOPLNIT !!!

